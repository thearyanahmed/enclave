/**
 * A pair of access and refresh tokens.
 *
 * @typedef {TokenPair} TokenPair
 * @property {string} access_token - Short-lived access token for API requests.
 * @property {string} refresh_token - Long-lived refresh token for obtaining new access tokens.
 * @property {number} expires_in - Access token expiry in seconds.
 */
