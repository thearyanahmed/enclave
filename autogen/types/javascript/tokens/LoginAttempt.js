/**
 * A recorded login attempt for rate limiting.
 *
 * @typedef {LoginAttempt} LoginAttempt
 * @property {string} email
 * @property {boolean} success
 * @property {(string | null)} [ip_address]
 * @property {string} attempted_at
 */
